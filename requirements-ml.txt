# ML-зависимости (T-09). Веса моделей этим файлом не качаются.
# Darwin: ставить целиком; если extra падает — ставить остальные секции отдельно.
# Docker ставит torch/torchaudio отдельно (CPU index или cu126), затем остальные строки.

torch==2.10.0
torchaudio==2.10.0
faster-whisper==1.2.1
pyannote.audio==4.0.7
gigaam[longform,torch] @ git+https://github.com/salute-developers/GigaAM.git
nemo_toolkit[asr]==3.0.0
